var router = require("../routes/")
var assert = require("assert")

describe('router', function()) {
    describe('get(/)', function() {
        it('Welcome to Weekly-Report', funxtion() {
            var msg = 'Weekly-Report';
            // assert.equal('Welcome to' + msg, router.)
        })
    })
}
